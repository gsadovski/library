%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% LIBRARY PREAMBULE                    %%
%% Author: Guilherme Sadovski           %%
%% Contact: guilhermesadovski@gmail.com %%
%% Last modified: 2023/10/23            %%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%
% STYLE DECLATION %
%%%%%%%%%%%%%%%%%%%

\ProvidesPackage{preambule}

%%%%%%%%
% FONT %
%%%%%%%%

% Only necessary for PDFLaTeX;
% provides font 8-bit enconding.
\usepackage[
  T1
]{fontenc}

% Manages typographical rules,
% and hyphenation patterns for
% the declared languages.
\usepackage[
  english
]{babel}

% Font family
\usepackage{
  lmodern,
}

% Provides better interwords spacing;
% compatible with babel settings.
\usepackage[
  babel,
]{microtype}

%%%%%%%%%%
% LAYOUT %
%%%%%%%%%%

\usepackage[
  paper=a4paper,
  top=20mm,
  bottom=20mm,
  twoside=true,
  left=35mm,
  right=25mm,
  headheight=15mm,
  headsep=5mm,
  footskip=15mm,
  includehead=true,
  includefoot=true,
  nomarginpar,
  verbose=true,
  %  showframe=true,
]{geometry}

% Removes foot indentation
\usepackage[
  hang
]{footmisc}

%%%%%%%%%%%%%%
% MULTIMEDIA %
%%%%%%%%%%%%%%

% Adds support for colors;
% compatible with hyperref
\usepackage[
  hyperref,
]{xcolor}

%%%%%%%%%%%%%%%%%%%%%%
% PACKAGE COLLECTION %
%%%%%%%%%%%%%%%%%%%%%%

\usepackage{
  amsmath,
  amsfonts,
  amssymb,
  amsthm,
  mathtools,
  mathrsfs,
  dsfont,
  wasysym,
  slashed,
  soul,
  tensor,
  csquotes,
  textcomp,
}

%%%%%%%%%%%%%%%%
% BIBLIOGRAPHY %
%%%%%%%%%%%%%%%%

\usepackage[
  backend=biber,
  style=reading,
  sorting=none,
  maxnames=4,
  minnames=1,
  hyperref=true,
  doi=true,
  eprint=true,
  isbn=true,
  url=false
]{biblatex}

\addbibresource{library.bib}

\DeclareFieldFormat{file}{\href{#1}{\textbf{Open file}}}

\nocite{*}

%%%%%%%%%%%%%%%%
% KOMA SCRIPTS %
%%%%%%%%%%%%%%%%

% Metadata
%\setkomafont{title}{\Huge\sffamily\bfseries}
%\setkomafont{subtitle}{\Large\itshape}
%\setkomafont{author}{\large\itshape}

% Headings
%\setkomafont{chapter}{\Huge\sffamily\bfseries}
%\setkomafont{section}{\Large\sffamily\bfseries}
%\setkomafont{subsection}{\normalfont\large\sffamily\itshape}
%\setkomafont{subsubsection}{\normalsize\normalfont\sffamily}

%%%%%%%%%%%%
% COMMANDS %
%%%%%%%%%%%%

% Angled brackets
\newcommand{\vev}[1]{
  \ifmmode\langle#1\rangle\else\textlangle#1\textrangle\fi
}

%%%%%%%%%%%%%%
% HYPERLINKS %
%%%%%%%%%%%%%%

\usepackage[
  colorlinks=true,
  urlcolor=blue,
  filecolor=blue,
  pdfstartpage=1,
  bookmarks=false
]{hyperref}
